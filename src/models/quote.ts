interface IQuote {
    id: string | null;
    text: string | null;
    storyId: string | null;
}

class Quote implements IQuote {
    id: string | null;
    text: string | null;
    storyId: string | null;

    // TODO add constructor
}

export default Quote;
